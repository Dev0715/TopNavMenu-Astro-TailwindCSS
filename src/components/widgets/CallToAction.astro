---
import type { CallToAction, Widget } from '~/types';
import Button from '~/components/ui/Button.astro';
import Image from '~/components/common/Image.astro';

interface Props extends Widget {
  action?: CallToAction;
}

const { action = {} } = Astro.props;
---

<div class="flex flex-col md:flex-row bg-secondary md:relative md:h-[27.3125rem]">
  <div class="basis-1/2 flex flex-col p-8 md:p-12 gap-3 md:gap-6">
    <div class="flex flex-col gap-3 md:gap-6">
      <h2 class="">Renewing the Energy Sector</h2>
      <p class="text-lg">
        Our goal is to be a trusted partner in your company's success, providing the tools you need to manage your
        resources effectively in the dynamic energy landscape. With Markular, you can focus on driving your business
        forward.
      </p>
    </div>
    <div>
      <Button
        text="Learn More"
        {...action || {}}
        variant="primary"
        class="mt-4 md:mb-0 text-base bg-white text-primary"
      />
    </div>
  </div>
  <div class="basis-1/2">
    <Image
      class="min-w-full object-cover"
      widths={[400, 768, 1024, 2040]}
      sizes="(max-width: 767px) 400px, (max-width: 1023px) 768px, (max-width: 2039px) 1024px, 2040px"
      loading="eager"
      width={640}
      height={437}
      src="~/assets/images/control_room.png"
      alt="Person in a control room"
    />
  </div>
</div>
