---
import { Icon } from 'astro-icon/components';
import Logo from '~/components/Logo.astro';
import { getHomePermalink } from '~/utils/permalinks';

interface Link {
  text?: string;
  href?: string;
  ariaLabel?: string;
  icon?: string;
}

interface Links {
  title?: string;
  links: Array<Link>;
}

export interface Props {
  links: Array<Links>;
  secondaryLinks: Array<Link>;
  socialLinks: Array<Link>;
  footNote?: string;
  theme?: string;
}

const { socialLinks = [], secondaryLinks = [], links = [], theme = 'light' } = Astro.props;

const currentYear = new Date().getFullYear();
---

<footer
  class:list={[
    { dark: theme === 'dark' },
    'bg-primary text-white relative border-t border-gray-200 dark:border-slate-800 not-prose',
  ]}
>
  <div class="dark:bg-dark absolute inset-0 pointer-events-none" aria-hidden="true"></div>
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 dark:text-white">
    <div class="flex flex-col md:flex md:flex-row gap-6 py-8 md:py-12">
      <div class="md:basis-2/3">
        <div class="mb-2">
          <a class="inline-block font-bold text-xl" href={getHomePermalink()}>
            <Logo class="text-white" />
          </a>
        </div>

        <div class="text-sm text-white flex flex-col gap-1 mb-6">
          <span class="font-semibold">Address:</span>
          <span>Nedre banegate 41, 4014 Stavanger, Norway</span>
        </div>

        <div class="text-sm text-white flex flex-col gap-1">
          <span class="font-semibold">Contact:</span>
          <span><a href="tel=+47 12345678">123-45-678</a></span>
          <span><a href="mailto:info@markular.com">info@markular.com</a></span>
        </div>

        <div class="md:flex md:items-center md:justify-start py-4">
          {
            socialLinks?.length ? (
              <ul class="flex mb-4 md:order-1 -ml-2 md:mb-0">
                {socialLinks.map(({ ariaLabel, href, text, icon }) => (
                  <li>
                    <a
                      class="text-white dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 focus:outline-none focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 rounded-lg text-sm p-2.5 inline-flex items-center"
                      aria-label={ariaLabel}
                      href={href}
                    >
                      {icon && <Icon name={icon} class="w-5 h-5" />}
                      <Fragment set:html={text} />
                    </a>
                  </li>
                ))}
              </ul>
            ) : (
              ''
            )
          }
        </div>
      </div>
      <div class="flex flex-col gap-12 md:basis-1/3 md:grid md:grid-cols-2 md:gap-2">
        {
          links.map(({ title, links }) => (
            <div class="">
              <div class="dark:text-white font-medium mb-2">{title}</div>
              {links && Array.isArray(links) && links.length > 0 && (
                <ul class="text-sm">
                  {links.map(({ text, href, ariaLabel }) => (
                    <li class="mb-2">
                      <a
                        class="text-white hover:text-gray-700 hover:underline dark:text-gray-400 transition duration-150 ease-in-out"
                        href={href}
                        aria-label={ariaLabel}
                      >
                        <Fragment set:html={text} />
                      </a>
                    </li>
                  ))}
                </ul>
              )}
            </div>
          ))
        }
      </div>
    </div>
    <div
      class="w-full text-white font-normal text-sm/[1.3125rem] flex flex-col-reverse md:flex md:flex-row md:justify-between gap-8 border-t border-secondary pt-6 pb-4 md:pt-8"
    >
      <span>© {currentYear} Markular. All rights reserved.</span>
      <div class="underline flex flex-col md:flex-row gap-4 md:gap-6">
        {
          secondaryLinks.map(({ text, href }) => (
            <>
              <a
                class="text-white hover:text-gray-700 dark:text-gray-400 hover:underline transition duration-150 ease-in-out"
                href={href}
                set:html={text}
              />
            </>
          ))
        }
      </div>
    </div>
  </div>
</footer>
